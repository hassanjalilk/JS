
// fortune teller

function tellFortune() {
	var noc = "No Idea";
	var pn = "No Clue";
	var gl = "Pak";
	var jt = "CEO maybe?!";
  return "I will be a " + jt + " in " + gl + ", and married to " + pn + " with " + noc + " kids!";
}

document.write(tellFortune());
console.log(tellFortune());



// age calculator

function ageCalculator() {
	var cy = 2014;
	var by = 1980;
	var byy = 1985;
	return "they are either " + (cy - by) + " or " + (cy - byy) + "";
}

document.write(ageCalculator());
console.log(ageCalculator());



// supply of your fav snack

function favoriteSnack() {
var currentAge = 23;
var maximumAge = 80;
var estimatedMeal = 5;
var totalDaysAYear = 365;
return "I will need " + ((maximumAge - currentAge) * estimatedMeal * totalDaysAYear) + " meals to last me until the ripe old age of " + maximumAge + ""

}

document.write(favoriteSnack());
console.log(favoriteSnack());



// calculating the circumference


function radiusA() {
var radius = 10;
return 2 * radius * Math.PI;
}

document.write(radiusA());
console.log(radiusA());



// calculating the area

function radiusA() {
var radius = 10;
return Math.PI * radius * radius;
}

document.write(radiusA());
console.log(radiusA());










